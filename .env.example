# OpenCode Harness Environment Configuration
# Copy this file to .env and adjust values as needed
#
# Note: This extension does not require environment variables.
# The opencode CLI (which this extension connects to) handles
# API keys and configuration via ~/.config/opencode/ or similar.
